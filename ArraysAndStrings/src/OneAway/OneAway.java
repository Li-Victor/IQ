package OneAway;

/*
 * Three types of edits are insert a character, replacing a character, and removing one character
 * Check to see if two strings are one edit away
 */
public interface OneAway {
	public boolean OneAway(String A, String B);
}
