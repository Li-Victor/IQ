package StringCompression;

/*
 * Perform basic string compression using counts of repeated characters
 * if compressed string is not smaller thatn the original string, return the original string
 * assume only uppercase and lowercase letters(a - z)
 */
public interface StringCompression {
	public String StringCompression(String A);
}
