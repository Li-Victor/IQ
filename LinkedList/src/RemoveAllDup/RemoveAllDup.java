package RemoveAllDup;

import Implementation.LinkedList;

/*
 * remove all duplicates in the linkedList
 * similar to removeDupMakeUnique problem
 * example: 1 2 1 -> 2
 */
public interface RemoveAllDup {
	public void removeAllDup(LinkedList l);
}
